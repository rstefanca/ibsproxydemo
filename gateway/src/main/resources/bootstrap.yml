spring:
  application:
    name: zuul-proxy
  cloud:
    config:
      failFast: true
      discovery:
        enabled: true

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/